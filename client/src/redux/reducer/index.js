const initialStore = {
  edarko: "edarko",
};

export const reducer = (store = initialStore, action) => {
  switch (action.type) {
    /* case value:
      return; */
    default:
      return store;
  }
};
